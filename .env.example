# Frontend API Key (used during build)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Optional: Full backend URL (e.g., https://api.example.com/api)
# Leave empty to use relative /api path (via proxy)
VITE_API_BASE_URL=
